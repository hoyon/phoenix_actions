name: Webpack build

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
      
      - name: webpack deploy
        run: |
          cd assets
          npm install
          npm run deploy
